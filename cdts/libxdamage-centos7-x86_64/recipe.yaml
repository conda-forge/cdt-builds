schema_version: 1

package:
  name: libxdamage-conda-x86_64
  version: 1.1.4

source:
  - url: https://vault.centos.org/7.9.2009/os/x86_64/Packages/libXdamage-1.1.4-4.1.el7.x86_64.rpm
    sha256: 790df35e712320c967e7934e417687e17457b7cf989e54045115dd8f4c560949
    target_directory: binary
    no_hoist: true
  # - url: https://vault.centos.org/7.9.2009/os/Source/SPackages/libXdamage-1.1.4-4.1.el7.src.rpm
  #   no_hoist: true
  #   folder: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: "distro != "\centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - "*"

requirements:
  host:
    - sysroot_linux-64 2.17.*
  run:
    - sysroot_linux-64 2.17.*

tests:
  script:
    - "echo "it installs!""

about:
  license: MIT
  license_file: {{ SRC_DIR }}/binary/usr/share/doc/libXdamage-1.1.4/COPYING
  summary: (CDT) X Damage extension library
  description: |
        X.Org X11 libXdamage runtime library.


  homepage: http://www.x.org

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
