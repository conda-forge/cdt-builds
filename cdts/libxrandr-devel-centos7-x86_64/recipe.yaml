schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: centos7

package:
  name: libxrandr-devel-conda-x86_64
  version: 1.5.1

source:
  - url: https://vault.centos.org/7.9.2009/os/x86_64/Packages/libXrandr-devel-1.5.1-2.el7.x86_64.rpm
    sha256: 2fa2bffa9cad18d1f30c6063e701cf89a915efcdad25f9552fab95a5ce4954f4
    target_directory: binary
  # - url: https://vault.centos.org/7.9.2009/os/Source/SPackages/libXrandr-1.5.1-2.el7.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - libxrandr-conda-x86_64 ==1.5.1 *_${{ cdt_build_number|int + 1000 }}
    - libarchive
  host:
    - libxrandr-conda-x86_64 ==1.5.1 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.17.*
  run:
    - libxrandr-conda-x86_64 ==1.5.1 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.17.*

tests:
  - script:
      - echo "it installs!"

about:
  homepage: http://www.x.org
  license: MIT
  license_file: libxrandr-license
  summary: (CDT) X.Org X11 libXrandr development package
  description: |
    X.Org X11 libXrandr development package

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
