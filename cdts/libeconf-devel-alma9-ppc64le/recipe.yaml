schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: alma9

package:
  name: libeconf-devel-conda-ppc64le
  version: 0.4.1

source:
  - url: https://repo.almalinux.org/almalinux/9.7/devel/ppc64le/os/Packages/libeconf-devel-0.4.1-4.el9.ppc64le.rpm
    sha256: f856836747f744908102ce5b839d9d12fe738de56d9c6c91c703ed56b07e4ae7
    target_directory: binary
  # - url: https://repo.almalinux.org/almalinux/9.7/devel/Source/Packages/libeconf-0.4.1-4.el9.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "alma9"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - libeconf-conda-ppc64le ==0.4.1 *_${{ cdt_build_number|int + 1000 }}
    - libarchive
  host:
    - libeconf-conda-ppc64le ==0.4.1 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.34.*
  run:
    - libeconf-conda-ppc64le ==0.4.1 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.34.*

tests:
  - script:
      - echo "it installs!"

about:
  homepage: https://github.com/openSUSE/libeconf
  license: MIT
  license_file: libeconf-devel-license
  summary: (CDT) Development files for libeconf
  description: |
    The libeconf-devel package contains libraries and header files for developing
    applications that use libeconf.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
