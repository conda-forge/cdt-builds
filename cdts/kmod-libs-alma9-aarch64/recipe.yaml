schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: alma9

package:
  name: kmod-libs-conda-aarch64
  version: 28

source:
  - url: https://repo.almalinux.org/almalinux/9.7/BaseOS/aarch64/os/Packages/kmod-libs-28-11.el9.aarch64.rpm
    sha256: 51db8f1e3ed367cdfcf8c6cbb053754c565cc498734e6d237a856cb2be574b10
    target_directory: binary
  # - url: https://repo.almalinux.org/almalinux/9.7/BaseOS/Source/Packages/kmod-28-11.el9.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "alma9"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - libarchive
  host:
    - sysroot_linux-aarch64 2.34.*
  run:
    - sysroot_linux-aarch64 2.34.*

tests:
  - script:
      - echo "it installs!"

about:
  homepage: https://git.kernel.org/pub/scm/utils/kernel/kmod/kmod.git
  license: LGPL-2.0-or-later
  license_file: kmod-copying
  summary: (CDT) Libraries to handle kernel module loading and unloading
  description: |
    The kmod-libs package provides runtime libraries for any application that
    wishes to load or unload Linux kernel modules from the running system.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
