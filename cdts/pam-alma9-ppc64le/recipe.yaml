schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: alma9

package:
  name: pam-conda-ppc64le
  version: 1.5.1

source:
  - url: https://repo.almalinux.org/almalinux/9.7/BaseOS/ppc64le/os/Packages/pam-1.5.1-26.el9_6.ppc64le.rpm
    sha256: 21df5046bcd4775bdba6729572e9027f9c6fa054b429f3904fa6a5f42a8a0b24
    target_directory: binary
  # - url: https://repo.almalinux.org/almalinux/9.7/BaseOS/Source/Packages/pam-1.5.1-26.el9_6.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "alma9"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - audit-libs-conda-ppc64le >=1.0.8 *_${{ cdt_build_number|int + 1000 }}
    - libeconf-conda-ppc64le >=0.3.5 *_${{ cdt_build_number|int + 1000 }}
    - libpwquality-conda-ppc64le >=0.9.9 *_${{ cdt_build_number|int + 1000 }}
    - libselinux-conda-ppc64le >=1.33.2 *_${{ cdt_build_number|int + 1000 }}
    - libarchive
  host:
    - audit-libs-conda-ppc64le >=1.0.8 *_${{ cdt_build_number|int + 1000 }}
    - libeconf-conda-ppc64le >=0.3.5 *_${{ cdt_build_number|int + 1000 }}
    - libpwquality-conda-ppc64le >=0.9.9 *_${{ cdt_build_number|int + 1000 }}
    - libselinux-conda-ppc64le >=1.33.2 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.34.*
  run:
    - audit-libs-conda-ppc64le >=1.0.8 *_${{ cdt_build_number|int + 1000 }}
    - libeconf-conda-ppc64le >=0.3.5 *_${{ cdt_build_number|int + 1000 }}
    - libpwquality-conda-ppc64le >=0.9.9 *_${{ cdt_build_number|int + 1000 }}
    - libselinux-conda-ppc64le >=1.33.2 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.34.*
tests:
  - script:
      - echo "it installs!"

about:
  homepage: http://www.linux-pam.org/
  license: LicenseRef-Callaway-BSD AND GPL-2.0-or-later
  license_file: binary/usr/share/licenses/pam/Copyright
  summary: (CDT) An extensible library which provides authentication for applications
  description: |
    PAM (Pluggable Authentication Modules) is a system security tool that allows
    system administrators to set authentication policy without having to recompile
    programs that handle authentication.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
