schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: alma9

package:
  name: kmod-devel-conda-x86_64
  version: 28

source:
  - url: https://repo.almalinux.org/almalinux/9.7/CRB/x86_64/os/Packages/kmod-devel-28-11.el9.x86_64.rpm
    sha256: ddbe32db3d9d2b0f5170e1920073df7c1ad503880bde140ed73cda39dc4fdde2
    target_directory: binary
  # - url: https://repo.almalinux.org/almalinux/9.7/CRB/Source/Packages/kmod-28-11.el9.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "alma9"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - kmod-libs-conda-x86_64 ==28 *_${{ cdt_build_number|int + 1000 }}
    - libarchive
  host:
    - kmod-libs-conda-x86_64 ==28 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.34.*
  run:
    - kmod-libs-conda-x86_64 ==28 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.34.*

tests:
  - script:
      - echo "it installs!"

about:
  homepage: https://git.kernel.org/pub/scm/utils/kernel/kmod/kmod.git
  license: GPL-2.0-or-later
  license_file: kmod-copying
  summary: (CDT) Header files for kmod development
  description: |
    The kmod-devel package provides header files used for development of
    applications that wish to load or unload Linux kernel modules.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
