schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: alma9

package:
  name: libsepol-devel-conda-ppc64le
  version: 3.6

source:
  - url: https://repo.almalinux.org/almalinux/9.7/AppStream/ppc64le/os/Packages/libsepol-devel-3.6-3.el9.ppc64le.rpm
    sha256: baee3f888da962702ee1a7a2b8858ad87c0c75a84eea32ebfdc6267d8ef83f46
    target_directory: binary
  # - url: https://repo.almalinux.org/almalinux/9.7/AppStream/Source/Packages/libsepol-3.6-3.el9.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "alma9"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - libsepol-conda-ppc64le ==3.6 *_${{ cdt_build_number|int + 1000 }}
    - libarchive
  host:
    - libsepol-conda-ppc64le ==3.6 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.34.*
  run:
    - libsepol-conda-ppc64le ==3.6 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.34.*

tests:
  - script:
      - echo "it installs!"

about:
  homepage: https://github.com/SELinuxProject/selinux/wiki
  license: LGPL-2.0-or-later
  license_file: libsepol-license
  summary: (CDT) Header files and libraries used to build policy manipulation tools
  description: |
    The libsepol-devel package contains the libraries and header files needed for
    developing applications that manipulate binary policies.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
