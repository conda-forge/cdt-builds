schema_version: 1

# TODO: hack to workaround https://github.com/conda-forge/rattler-build-conda-compat/issues/112
context:
  cdt_build_number: 111
  distro: centos7

package:
  name: xcb-util-renderutil-devel-conda-aarch64
  version: 0.3.9

source:
  - url: https://vault.centos.org/altarch/7.9.2009/os/aarch64/Packages/xcb-util-renderutil-devel-0.3.9-3.el7.aarch64.rpm
    sha256: f320115bbf90befe8e15ce032ac02962f57247960742dc6e0cda10a685279fcc
    target_directory: binary
  # - url: https://vault.centos.org/altarch/7.9.2009/os/Source/SPackages/xcb-util-renderutil-0.3.9-3.el7.src.rpm
  #   target_directory: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: distro != "centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - '*'

requirements:
  build:
    - xcb-util-renderutil-conda-aarch64 ==0.3.9 *_${{ cdt_build_number|int + 1000 }}
    - libarchive
  host:
    - xcb-util-renderutil-conda-aarch64 ==0.3.9 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-aarch64 2.17.*
  run:
    - xcb-util-renderutil-conda-aarch64 ==0.3.9 *_${{ cdt_build_number|int + 1000 }}
    - sysroot_linux-aarch64 2.17.*

tests:
  - script:
      - echo "it installs!"

about:
  homepage: http://xcb.freedesktop.org
  license: MIT
  license_file: xcb-util-renderutil-license
  summary: (CDT) Development and header files for xcb-util-renderutil
  description: |
    Development files for xcb-util-renderutil.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
