schema_version: 1

package:
  name: xorg-x11-server-common-conda-x86_64
  version: 1.20.4

source:
  - url: https://vault.centos.org/7.9.2009/os/x86_64/Packages/xorg-x11-server-common-1.20.4-10.el7.x86_64.rpm
    sha256: 58d06cf5c81422503e12b906cbcb3ac6e227367ea5ea8cae773bb77cc6aecce7
    target_directory: binary
    no_hoist: true
  # - url: https://vault.centos.org/7.9.2009/os/Source/SPackages/xorg-x11-server-1.20.4-10.el7.src.rpm
  #   no_hoist: true
  #   folder: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: "distro != "\centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - "*"

requirements:
  build:
    - pixman-conda-x86_64 >=0.30.0 *_{{ cdt_build_number|int + 1000 }}
  host:
    - pixman-conda-x86_64 >=0.30.0 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.17.*
  run:
    - pixman-conda-x86_64 >=0.30.0 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.17.*

tests:
  script:
    - "echo "it installs!""

about:
  license: MIT
  license_file: {{ SRC_DIR }}/binary/usr/share/doc/xorg-x11-server-common-1.20.4/COPYING
  summary: (CDT) Xorg server common files
  description: |
        Common files shared among all X servers.


  homepage: http://www.x.org

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
