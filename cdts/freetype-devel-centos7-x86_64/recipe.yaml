schema_version: 1

package:
  name: freetype-devel-conda-x86_64
  version: 2.8

source:
  - url: https://vault.centos.org/7.9.2009/os/x86_64/Packages/freetype-devel-2.8-14.el7.x86_64.rpm
    sha256: 04d7a547ed0ce42e81625ec7176e927f9f8d91d3d22f961cc8814547b95cdec5
    target_directory: binary
    no_hoist: true
  # - url: https://vault.centos.org/7.9.2009/os/Source/SPackages/freetype-2.8-14.el7.src.rpm
  #   no_hoist: true
  #   folder: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: "distro != "\centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - "*"

requirements:
  build:
    - freetype-conda-x86_64 ==2.8 *_{{ cdt_build_number|int + 1000 }}
  host:
    - freetype-conda-x86_64 ==2.8 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.17.*
  run:
    - freetype-conda-x86_64 ==2.8 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.17.*

tests:
  script:
    - "echo "it installs!""

about:
  license: (FTL or GPLv2+) and BSD and MIT and Public Domain and zlib with acknowledgement
  license_file: {{ SRC_DIR }}/binary/LICENSE_NOT_FOUND
  summary: (CDT) FreeType development libraries and header files
  description: |
        The freetype-devel package includes the static libraries and header files for
        the FreeType font rendering engine.  Install freetype-devel if you want to
        develop programs which will use FreeType.


  homepage: http://www.freetype.org

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
