package:
  name: audit-libs-devel-conda-x86_64
  version: 3.1.2

source:
  - url: https://repo.almalinux.org/almalinux/8.10/BaseOS/x86_64/os/Packages/audit-libs-devel-3.1.2-1.el8_10.1.x86_64.rpm
    sha256: e3fbf8050d8004f1aedf014282f0b4f5bdfa6d2d61ea2866dcf93b6573b69f16
    no_hoist: true
    folder: binary
  # - url: https://repo.almalinux.org/almalinux/8.10/BaseOS/Source/Packages/audit-3.1.2-1.el8_10.1.src.rpm
  #   no_hoist: true
  #   folder: source

build:
  number: {{ cdt_build_number|int + 1000 }}
  noarch: generic
  binary_relocation: false
  detect_binary_files_with_prefix: false
  missing_dso_whitelist:
    - '*'
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  skip: true  # [distro != "alma8"]

requirements:
  build:
    - audit-libs-conda-x86_64 ==3.1.2 *_{{ cdt_build_number|int + 1000 }}
  host:
    - audit-libs-conda-x86_64 ==3.1.2 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.28.*
  run:
    - audit-libs-conda-x86_64 ==3.1.2 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-64 2.28.*
test:
  commands:
    - echo "it installs!"

about:
  home: http://people.redhat.com/sgrubb/audit/
  license: LGPL-2.0-or-later
  license_family: LGPL
  license_file: audit-libs-copying.lib
  summary: (CDT) Header files for libaudit
  description: |
    The audit-libs-devel package contains the header files needed for developing
    applications that need to use the audit framework libraries.

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
