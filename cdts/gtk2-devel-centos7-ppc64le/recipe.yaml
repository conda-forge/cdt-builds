schema_version: 1

package:
  name: gtk2-devel-conda-ppc64le
  version: 2.24.31

source:
  - url: https://vault.centos.org/altarch/7.9.2009/os/ppc64le/Packages/gtk2-devel-2.24.31-1.el7.ppc64le.rpm
    sha256: 0f1b9e137832c570a476959cb0979747799e5e98865fc395059cf56267b122be
    target_directory: binary
    no_hoist: true
  # - url: https://vault.centos.org/altarch/7.9.2009/os/Source/SPackages/gtk2-2.24.31-1.el7.src.rpm
  #   no_hoist: true
  #   folder: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: "distro != "\centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - "*"

requirements:
  build:
    - atk-devel-conda-ppc64le >=1.29.4 *_{{ cdt_build_number|int + 1000 }}
    - cairo-devel-conda-ppc64le >=1.6.0 *_{{ cdt_build_number|int + 1000 }}
    - glib2-devel-conda-ppc64le >=2.28.0 *_{{ cdt_build_number|int + 1000 }}
    - gtk2-conda-ppc64le ==2.24.31 *_{{ cdt_build_number|int + 1000 }}
    - pango-devel-conda-ppc64le >=1.20.0 *_{{ cdt_build_number|int + 1000 }}
  host:
    - atk-devel-conda-ppc64le >=1.29.4 *_{{ cdt_build_number|int + 1000 }}
    - cairo-devel-conda-ppc64le >=1.6.0 *_{{ cdt_build_number|int + 1000 }}
    - glib2-devel-conda-ppc64le >=2.28.0 *_{{ cdt_build_number|int + 1000 }}
    - gtk2-conda-ppc64le ==2.24.31 *_{{ cdt_build_number|int + 1000 }}
    - pango-devel-conda-ppc64le >=1.20.0 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.17.*
  run:
    - atk-devel-conda-ppc64le >=1.29.4 *_{{ cdt_build_number|int + 1000 }}
    - cairo-devel-conda-ppc64le >=1.6.0 *_{{ cdt_build_number|int + 1000 }}
    - glib2-devel-conda-ppc64le >=2.28.0 *_{{ cdt_build_number|int + 1000 }}
    - gtk2-conda-ppc64le ==2.24.31 *_{{ cdt_build_number|int + 1000 }}
    - pango-devel-conda-ppc64le >=1.20.0 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-ppc64le 2.17.*

tests:
  script:
    - "echo "it installs!""

about:
  license: LGPL-2.0-or-later
  license_file: {{ SRC_DIR }}/binary/LICENSE_NOT_FOUND
  summary: (CDT) Development files for GTK+
  description: |
        This package contains the libraries and header files that are needed for
        writing applications with the GTK+ widget toolkit. If you plan to develop
        applications with GTK+, consider installing the gtk2-devel-docs package.


  homepage: http://www.gtk.org

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
