schema_version: 1

package:
  name: cracklib-devel-conda-aarch64
  version: 2.9.0

source:
  - url: https://vault.centos.org/altarch/7.9.2009/os/aarch64/Packages/cracklib-devel-2.9.0-11.el7.aarch64.rpm
    sha256: 1f5c7fea789a19adf5196b6937b6a93db8e4949fc56ea3a86f4d0770b808393f
    target_directory: binary
    no_hoist: true
  # - url: https://vault.centos.org/altarch/7.9.2009/os/Source/SPackages/cracklib-2.9.0-11.el7.src.rpm
  #   no_hoist: true
  #   folder: source

build:
  # this skip is here because we need different package hashes per distro.
  # we therefore list all possible values in CBC and skip those we don't want;
  # use in a selector ensures that the `distro` variable shows up in the hash
  number: ${{ cdt_build_number|int + 1000 }}
  skip: "distro != "\centos7"
  noarch: generic
  prefix_detection:
    ignore_binary_files: false
  dynamic_linking:
    binary_relocation: false
    missing_dso_allowlist:
      - "*"

requirements:
  build:
    - cracklib-conda-aarch64 ==2.9.0 *_{{ cdt_build_number|int + 1000 }}
  host:
    - cracklib-conda-aarch64 ==2.9.0 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-aarch64 2.17.*
  run:
    - cracklib-conda-aarch64 ==2.9.0 *_{{ cdt_build_number|int + 1000 }}
    - sysroot_linux-aarch64 2.17.*

tests:
  script:
    - "echo "it installs!""

about:
  license: LGPL-2.0-or-later
  license_file: {{ SRC_DIR }}/binary/LICENSE_NOT_FOUND
  summary: (CDT) Development files needed for building applications which use cracklib
  description: |
        The cracklib-devel package contains the header files and libraries needed for
        compiling applications which use cracklib.


  homepage: http://sourceforge.net/projects/cracklib/

extra:
  recipe-maintainers:
    - conda-forge/help-cdts
